import * as three from 'three';
declare global {
    var THREE: typeof three;
}
interface Attribute {
    name: string;
    data: (i: number, total: number) => void;
    size: number;
}
declare class Phenomenon {
    constructor(geometry: three.Geometry, material: three.Material, multiplier: number, attributes: Array<Attribute>, uniforms: object, vertex: string, castShadow?: boolean, fragment?: Array<Array<string>>);
}
export default Phenomenon;
